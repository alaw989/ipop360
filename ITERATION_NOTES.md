# Iteration Notes

## Goal
Fix VenuePipeline::mergeVenues field-fold correctness (spec-094, see specs/094-venueshape-contract-mergevenues-fold.md for full context): the fold gate only takes a source field when the target value is null, but free-source normalizers (Overpass/Socrata/BizData) default google_review_count to 0 not null, so merging a SerpApi-rated row onto a free-source target silently keeps review_count=0 and PopularityScoreService shrinks the Bayesian quality to the mean, discarding a real rating. Fix: treat review_count 0 as foldable (prefer max(target,source)), and add website_url/opening_hours to the fold field allowlist (VenuePipeline.php ~line 217-247) since they are currently dropped on fold. Define a VenueShape contract (typed-array/docblock) that every per-source normalizers normalizeResults and normalizeForEnrichment output must satisfy, and add an invariant test. Add the 4 merge test assertions spec-094 specifies: (a) source-rated to target-null/0-rating fold, (b) target-preferred-when-both-present, (c) review-count 0-to-N fold, (d) website_url carry onto a name-only target — confirm commenting out the fold fix turns these tests red. Then, as a bounded follow-up (do not skip, but do not over-build): write a throwaway artisan command that pulls cached raw per-source rows from the external_api_cache table for a few already-searched cities, runs them through VenuePipeline::crossSourceDedup/venuesMatch/mergeVenues exactly as the live path does, and reports (1) match/no-match pairs near the 85% name-similarity / 0.2km radius / phone-last-10-digit thresholds, (2) any row where a merge caused a field to regress from non-null/non-zero to null/zero (checks whether other fields beyond review_count have the same gate bug), (3) name/phone/distance conflicts among matched pairs near threshold edges. This is read-only investigation — do not build a persistent tool, keep it minimal, and write findings as a short markdown summary rather than new specs unless a concrete new bug is found.

## State
(empty — first iteration will start the log)

## Log
