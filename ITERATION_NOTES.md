# Iteration Notes

## Goal
Fix spec-100 (specs/100-architecture-cleanup.md), NARROWED + CORRECTED SCOPE — verified against current code on 2026-09-01. This spec is from 2026-06-30 and has drifted; 2 of its dead-code claims are FALSE for the current codebase and its item 3 (merging ~135 LOC of fetch-orchestration between LiveSearchService and RestaurantEnrichmentService) is deliberately OUT OF SCOPE for this run (too high-risk for automated handling, deferred to a separate follow-up) — do NOT touch LiveSearchService::dispatchPool/buildPoolRequest or RestaurantEnrichmentService::fetchAndNormalizeAllSources, and do NOT move/consolidate haversineKm. Do these 3 items only: (1) CACHE API UNIFICATION: app/Models/ExternalApiCache.php has two parallel cache APIs — get()/put() (~line 45-66) and findByKey()/storeByKey() (~line 75-91). app/Services/WikidataService.php is the only remaining get()/put() caller (4 call sites: ~lines 48,76,198,226, using ExternalApiCache::get('wikidata', $cacheId) / ::put('wikidata', $cacheId, $venues, self::TTL_HOURS)). Migrate WikidataService to storeByKey('wikidata:'.$cacheId, $venues, $expiresAt) / findByKey('wikidata:'.$cacheId) — storeByKey takes a Carbon expiry not a TTL-hours int, so convert self::TTL_HOURS via now()->addHours(self::TTL_HOURS). Then delete ExternalApiCache::get()/put() entirely (confirm zero other callers first via grep — WikidataService is the only one). Add an invariant test asserting Wikidata cache writes/reads round-trip correctly through the unified API (a cache write then a read within TTL returns the same data; verify Michelin-award-lookup behavior is unchanged — check tests/ for existing WikidataService/RefreshAwards tests first and extend rather than duplicating). (2) SNAPSHOT SERVICE EXTRACTION: app/Http/Controllers/RestaurantController.php hand-builds ExternalApiCache keys and reads/writes ExternalApiCache directly in 3 places: snapshotLiveResults() (~line 96, writes 'preview:{slug}' keys via ExternalApiCache::storeByKey — NOTE: PR #161/spec-095 already wrapped this loop in a DB::transaction, keep that), a preview-reading method (search for 'preview:' key reads, ~line 240ish), and apiIndex() (~line 282-335, which may build/read 'live_page:' keys — verify the exact current shape by reading the file, it may have shifted since spec-095's changes). Extract a new app\Services\LiveSearchSnapshotService with methods like storePageSnapshot()/readPageSnapshot()/storePreview()/readPreview() that wrap the ExternalApiCache calls; RestaurantController becomes a thin caller injecting this new service. Zero behavior change — same cache keys, same TTLs, same DB::transaction wrapping from spec-095. Add a unit test for the new service in isolation (mock/fake ExternalApiCache interactions) plus confirm existing RestaurantControllerTest cases covering preview/snapshot/apiIndex still pass unmodified (their assertions shouldn't need to change if behavior is truly preserved). (3) CORRECTED DEAD-CODE DELETION — the spec's original claims about buildCacheKey and lib/api.ts are WRONG, do not delete those two: app/Services/RestaurantEnrichmentService::buildCacheKey (~line 339) IS called (from a match/switch default case ~line 301) — it is LIVE code, leave it alone. resources/js/lib/api.ts is imported by resources/js/Components/LocationPicker.vue AND resources/js/composables/useGeolocation.ts (confirmed via grep) — it is LIVE code, leave it and its test alone. Only delete what's CONFIRMED actually dead: (a) the genuinely no-op migration database/migrations/2026_06_20_141940_add_score_breakdown_to_restaurants_table.php (confirmed: both up() and down() are empty Schema::table closures with only a comment; the real column-add migration is the sibling 2026_06_20_141943_add_score_breakdown_to_restaurants_table.php which stays) — delete the 141940 file only, run migrate:fresh to confirm nothing breaks. (b) app/Services/RestaurantEnrichmentService::normalizeOverpassWithFallback is ALREADY GONE (confirmed via grep, already removed by an earlier PR) — nothing to do here, skip. (c) The 4 free-source services (BizDataApiService, OverpassService, SocrataOpenDataService, SerpApiService) each have a public search() method (confirmed via grep: ZERO callers anywhere in app/ or tests/ for all 4 search() methods — fully dead) — delete all 4 search() methods. Each service also has a public fetchRaw() method: BizDataApiService/OverpassService/SocrataOpenDataService's fetchRaw() have ZERO callers anywhere (confirmed via grep) — delete those 3. SerpApiService::fetchRaw() IS called, but ONLY from tests/Feature/SerpApiExhaustionTest.php and tests/Feature/SerpApiQuotaGuardTest.php (7 call sites total, testing provider-exhaustion/quota-guard detection — genuinely important behavior, not throwaway coverage) — before deleting SerpApiService::fetchRaw(), check whether the SAME exhaustion/quota-guard detection logic it exercises is also reachable and already covered via the production pool-based path (consumePoolResponses/dispatchPool in LiveSearchService, WITHOUT modifying those methods — just check if calling through the pool triggers the same detection code fetchRaw() shares). If the exhaustion logic is a shared private helper that both fetchRaw() and the pool path call, you can safely delete fetchRaw() and port those 7 test call sites to drive the same shared logic through the pool API instead (e.g. via consumePoolResponses with a faked pool response), preserving equivalent coverage. If porting is not straightforward or the coverage would be weaker, LEAVE SerpApiService::fetchRaw() in place (do not force it) and just delete the other 3 services' dead search()+fetchRaw() (6 methods total) — note in ITERATION_NOTES.md which choice you made and why. Run PHPStan + the full backend suite after every deletion to catch any missed caller. All 3 items are backend-only (PHP) — do not touch frontend/resources/js files. This spec explicitly targets the SerpApi-quota-adjacent read/enrichment paths — be conservative, verify zero behavior change at every step, and if anything is ambiguous or risky, prefer leaving code in place over a guessed deletion.

## State
Item 1 + 2 + 3 DONE. Item 3: deleted no-op migration 141940 AND all 4 services' dead fetchRaw() (BizData/Overpass/Socrata/SerpApi); search() kept (live-tested, NOT dead). Full backend suite green (1112 passed), phpstan + pint clean.
SerpApiService::fetchRaw() removed: 7 test call sites ported (429-exhaustion → consumePoolResponses; repeated-calls undercount + failed-call accounting → search()), 2 redundant fetchRaw tests dropped, shouldNotReceive('fetchRaw') guard removed from LiveSearchScoringTest.
Goal is 3/3 complete — no improvement remains.

## Log
- [4] Item 3 complete: deleted SerpApiService::fetchRaw() (last dead method); ported 7 test call sites to pool/search, dropped 2 redundant tests, removed shouldNotReceive guard. Full suite green (1112), phpstan + pint clean.
- [3] Item 3 partial: deleted no-op migration 141940 + dead fetchRaw() from BizData/Overpass/Socrata; corrected goal (search() is live-tested, not dead). 1114 passed, phpstan clean.
- [2] Item 2 done: extracted LiveSearchSnapshotService, controller no longer touches ExternalApiCache; 8 new unit tests; RestaurantControllerTest+RestaurantPreviewTest pass unmodified.
- [1] Item 1 done: WikidataService now uses storeByKey('wikidata:'.$cacheId, …, now()->addHours(TTL_HOURS)) / findByKey('wikidata:'.$cacheId); deleted ExternalApiCache::get()/put(); dropped 2 put() tests from ExternalApiCacheEmptyTtlTest, updated serves_from_cache_only to storeByKey, added test_award_cache_round_trips_through_unified_key_api.
