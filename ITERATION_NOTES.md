# Iteration Notes

## Goal
Fix spec-102 (specs/102-test-coverage-backfill.md), NARROWED + CORRECTED against current code as of 2026-09-02 (the spec is from 2026-06-30; the codebase has moved since — verify every claim yourself before writing a test, several have drifted). Do these 4 items:

(1) [P2] QUALITY x CUISINE_MATCH E2E GAP — STILL VALID. tests/Unit/LiveSearchScoringTest.php has exactly two tests that set Config::set('services.serpapi.api_key', 'test-key') (test_live_result_with_google_bonus_signals and test_quality_signal_active_when_serpapi_key_configured, both around lines 218-283) but BOTH call PopularityScoreService::calculateBreakdownForArray() directly on a single hand-built row — neither runs the full LiveSearchService::search() orchestration on a CUISINE-SCOPED query with the key set, so the prod-only interaction between the Quality signal (weight 0.60, gated inactive without a configured rating source) and the Cuisine Match signal (weight 0.15, spec-071) is never exercised together through the real pipeline. There is a separate cuisine-scoped orchestration test (test_scoped_search_stamps_cuisine_match_and_confidence_filters_output, ~line 1753) but it never sets the serpapi key and its venues carry no ratings, so Quality is inactive there too. Add ONE new test in LiveSearchScoringTest.php: set Config::set('services.serpapi.api_key','test-key'), seed a cuisine (e.g. brazilian per the spec's suggestion, using the existing seedCuisine() helper other tests in this file use), build two serpapi venues via makeServiceWithVenues() — one with a genuine cuisine-keyword name/description match but a LOWER rating (e.g. 4.0/200 reviews), one with NO cuisine keyword match but a HIGHER rating (e.g. 4.9/2000 reviews) — call search(lat,lng,'brazilian'), and assert the eventual winner's score_breakdown signals array contains BOTH 'Quality' and 'Cuisine Match' labels, pinning which venue actually wins (the point is a mutation check: if you commented out the cuisine_match weight or the quality gate, this specific test would go red — reason through the actual weights in config/restaurant-finder.php to pick numbers where the interaction is what decides the winner, don't just assert both labels exist without the order actually depending on both).

(2) [P3] apiIndex LIVE RESPONSE SHAPE — STILL VALID, confirmed unchanged. tests/Feature/RestaurantControllerTest.php's test_api_live_sort_preserves_response_shape (~line 669) only asserts is_live/total/next_page_url/data-count — it never inspects an individual row's fields. Extend it (or add a sibling test) to assert $response->json('data.0.google_rating'), 'data.0.source', 'data.0.score_breakdown', and 'data.0.popularity_score' are all present and correctly shaped for a rated live row (use the existing liveRow() test helper with google_rating/source set, per how other tests in this file already build fixture rows).

(3) [P3] useFavorites GUEST->AUTHED MERGE ROUND-TRIP — STILL VALID, confirmed unchanged. resources/js/composables/__tests__/useFavorites.spec.ts only unit-tests getLocalFavoritesForMerge() in isolation (splitting ids from un-persisted venues, ~line 121). The actual merge-on-login flow — checking localStorage, calling getLocalFavoritesForMerge(), POSTing {ids, venues} to /favorites/merge, and clearing localStorage on success — lives as inline top-level script in resources/js/app.ts (~lines 19-69), which is currently untestable in isolation because it's boot-time code in the app entry file, not an exported function. To close this gap, extract that merge-on-login logic out of app.ts into a small exported function (e.g. mergeFavoritesOnLogin() in resources/js/composables/useFavorites.ts, or a new resources/js/lib/mergeFavoritesOnLogin.ts — your call, keep it minimal) that app.ts then just calls; this is a reasonable minimal refactor to make currently-untestable logic testable, not scope creep. Then add a vitest spec exercising the real round-trip: seed localStorage with 2 local favorites, mock axios.post, call the extracted function, assert the POST body sent to /favorites/merge matches {ids: [...], venues: [...]} matching what app/Http/Controllers/FavoriteController::merge()'s validation expects (check that controller's validate() rules first so the test fixture shape is correct), and assert localStorage is cleared after a successful resolve. Also add one failure-path case (POST rejects) asserting localStorage is NOT cleared.

(4) [P3] NAME-DENYLIST RECALL EDGE — THE SPEC'S ORIGINAL PREMISE IS NOW FALSE, deliberately corrected here: spec-102 claims "the name-denylist is serpapi-only" and asks for a test proving a bizdata row survives it. That is no longer true — app/Services/VenuePipeline.php's nameLooksNonRestaurant() (called from filterNonRestaurants(), ~line 644-682) is now gated on `in_array($source, ['serpapi', 'bizdata'], true)`, i.e. BOTH serpapi and bizdata are checked (extended by the later "Southern Bail Bonds Dallas" fix — see tests/Unit/VenuePipelineNonRestaurantTest.php, which already pins an untyped bizdata "Southern Bail Bonds Dallas" row being correctly DROPPED). Do NOT write the test as originally specified (it would assert the OPPOSITE of current correct behavior). Instead: (a) check whether VenuePipelineNonRestaurantTest.php (or any other test) already pins the RECALL-protective direction for bizdata specifically — an untyped bizdata row with a genuine food-sounding name (no denylist word) surviving filterNonRestaurants(). If that specific case isn't already covered, add it (a bizdata source, empty place_types, ordinary restaurant name — assert it's kept, not dropped). (b) Separately, audit app/Services/VenuePipeline.php's NAME_NON_RESTAURANT_PATTERNS constant (~line 111: 'wax', 'waxing', 'bail bond', 'bail bonds', 'attorney', 'notary', 'insurance agency') for real-world substring collisions the same way spec-046 already found for cuisine keywords (see the project's own established method: cross-check each entry against plausible real restaurant/place names, not just test fixtures — e.g. does 'wax' collide with a real place name like a city called "Waxahachie" that a restaurant might legitimately be named after, the way 'spa' collided with 'spanish'). If you find a genuine collision, decide case-by-case whether to fix it (e.g. word-boundary match instead of raw str_contains for that specific entry) and add a regression test proving the fix (mirroring how CuisineMatcher/config/cuisine-keywords.php handle this class of bug elsewhere in the codebase) — but only fix what you can concretely demonstrate collides with a plausible real venue name; do not invent a fix for a collision you can't demonstrate. If you find no genuine collision after a real audit, say so explicitly in ITERATION_NOTES.md and move on — a documented "audited, no collision found" is an acceptable outcome for this sub-item, do not force a speculative change.

SKIP entirely (already done, confirmed on current master — do not re-touch): the spec's "Vitest gaps" item (useGeolocation.spec.ts, useCardGallery.spec.ts, StarRating/DetailMap/CuisinePicker/CardGallery component specs) — all of these files already exist on master with real test coverage (verified via find before this goal was written). If you notice a genuine hole within one of them while working on it, that's fine to note, but do not treat "write these files from scratch" as part of this goal.

General constraints: this goal touches BOTH backend (PHP/PHPUnit) and frontend (Vue/vitest) — that's expected, items 1/2/4 are backend-only, item 3 is frontend-only (plus reading the backend FavoriteController for the validation contract). Run both composer test and npm run test after every meaningful change, plus PHPStan for any PHP changes. This is test-coverage backfill, not a refactor pass — keep production-code changes minimal (item 3's small extraction, and item 4's possible one-line word-boundary fix, are the only production-code touches anticipated; everything else should be test-file-only). If anything turns out more invasive than described, prefer a smaller conservative version and note the deviation + why in ITERATION_NOTES.md, same as prior spec loops (100, 101) have done.

## State
Done (1) (2) (3). (3): extracted merge-on-login out of app.ts into exported `mergeFavoritesOnLogin()` (new resources/js/lib/mergeFavoritesOnLogin.ts, returns Promise<void>, swallows errors same as before); app.ts now just imports + calls it. New spec resources/js/lib/__tests__/mergeFavoritesOnLogin.spec.ts (3 tests: split {ids,venues} POST + clear on success; keep storage on reject; no-op unauthed) uses vi.resetModules()+dynamic import to reset the module-level hasCheckedMerge flag. POST body matches FavoriteController::merge() validation (venues.*.name required, slug nullable, ids.* integer). vitest 1082 passed; vue-tsc clean.
Next: item (4) denylist audit — (a) bizdata RECALL-protective survival test if not already covered, (b) NAME_NON_RESTAURANT_PATTERNS substring-collision audit ('wax' vs 'Waxahachie' etc.) + word-boundary fix if warranted.

## Log
- 2026-09-02: Item (3) done — mergeFavoritesOnLogin() extraction + round-trip spec.
- 2026-09-02: Item (2) done — rated-row live response-shape test.
- 2026-09-02: Item (1) done — Quality×Cuisine Match E2E test (weights 0.35/0.50, not spec's 0.60/0.15).
