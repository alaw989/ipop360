# Iteration Notes

## Goal
Fix spec-097 (specs/097-config-and-regex-drift-guards.md), UPDATED CONTEXT — verified against current code on 2026-09-01: config/restaurant-finder.php has grown to 877 lines and the pattern-building code lives in app/Services/CuisineMatcher.php (NOT LiveSearchService as the spec's stale text says — CuisineMatcher is now the single accessor for config/cuisine-keywords.php). Both items are purely additive new test files, no runtime behavior change. Do both: (1) Regex compile drift-guard: CuisineMatcher::matchesEvidence() (~line 172) builds a pattern via '/'.implode('|', $this->keywordsFor([$cuisineSlug])).'/i' — the keywords in config/cuisine-keywords.php intentionally use raw '.' as a regex separator-wildcard (NOT preg_quoted, documented at the top of that config file — do not add preg_quote, that would regress recall) so a future malformed keyword (e.g. 'pho+', unbalanced parens/brackets) would silently break preg_match with a compile error. Write tests/Unit/CuisineKeywordRegexGuardTest.php: iterate every cuisine slug in config/cuisine-keywords.php (or via CuisineMatcher's own accessor), build the pattern exactly as matchesEvidence() does, run @preg_match($pattern, '') and assert preg_last_error() === PREG_NO_ERROR (and the pattern isn't false) — name the offending cuisine slug in the assertion failure message so a bad keyword is instantly locatable. Also cover CuisineMatcher::matchesRivalEvidence()'s combined cross-cuisine rival pattern (~line 223) the same way. Prove the test is meaningful: temporarily add a malformed keyword (e.g. 'pho+(') to a copy/fixture and confirm the test fails, then revert — do not leave the malformed keyword in the committed config. (2) Config invariant test: config/restaurant-finder.php's top-level keys are: cities, city_states, cuisines (all three are DATA CATALOGS — lat/lng coordinate lists and name mappings, NOT tunable knobs, EXCLUDE them entirely from any invariant scan), then the actual operational knob sections: ranking, live_search, cache, sources, serpapi, free_sources, enrich, dedup, filters, website_scraper, require_verified_social_links (a standalone top-level bool), trending, homepage, favorites. Write tests/Unit/RestaurantFinderConfigInvariantTest.php that walks ONLY those operational sections (skip cities/city_states/cuisines) and asserts sane types/ranges per the spec's heuristic: values whose key suggests a kill-switch/flag (contains 'enabled', 'require_', 'guard', or is a literal bool) must be actual booleans; values whose key suggests a weight/fraction/score (e.g. under a 'weights' sub-array, or containing 'weight'/'fraction'/'threshold' where the value is a float between 0 and 1 in current config) must be numeric in [0,1]; values whose key suggests a limit/cap/count/ttl/budget must be positive integers. Do not force a rule onto a value that doesn't fit one of these categories — inspect the actual current values first (read the file) and write assertions that reflect the REAL structure, not a guess; if a knob doesn't cleanly fit a category, either skip it with a comment explaining why or add a narrower per-key assertion. The test must pass on the current (clean) config as-is. All existing backend tests must stay green — this is two new additive test files only, do not modify any non-test production code.

## State
- Both spec-097 items DONE. Part (2): tests/Unit/RestaurantFinderConfigInvariantTest.php — walks the 14 operational sections (excludes cities/city_states/cuisines), asserts flags are bool, weights/fractions/thresholds in [0,1], limits positive; string/null knobs allowlisted (STRING_KNOBS/NULL_KNOBS); list-arrays (amenities/mirrors) skipped.
- Proven meaningful: temp RANK_WEIGHT_QUALITY=1.5 → red [0,1]; temp (int) SERPAPI_READ_PATH_GUARD → red bool; reverted. File read via env-scrubbed require so .env can't mask committed defaults.
- Next: nothing — goal complete. Full suite green (1111), pint + phpstan clean on new test.

## Log
- 2026-09-01: added RestaurantFinderConfigInvariantTest (top-level key set + per-knob type/range over 14 operational sections); verified red on weight-out-of-range and non-bool kill-switch mutations, reverted; 2 tests pass (102 assertions), pint + phpstan clean.
- 2026-09-01: added CuisineKeywordRegexGuardTest (on + rival pattern compile checks); verified red on temp malformed keyword, reverted; 2 tests pass (178 assertions), pint clean.

---

## Goal (merged from master — spec-096, already shipped)
Fix spec-096 (specs/096-scheduled-job-observability.md), UPDATED SCOPE — verified against current code on 2026-09-01, 5 of the original spec's 6 items are already done by later work and must NOT be re-touched: (1) failure hooks — every scheduled job in routes/console.php already has ->onFailure() logging to Log::channel('enrichment') plus SchedulerTelemetry::attach(), and the scheduled scheduler:health command already emails ADMIN_NOTIFY_EMAILS on problems (a better mechanism than the spec's proposed emailOutputOnFailure, do not add that). (2) empty-path SUCCESS — ScoreRestaurants.php already returns SUCCESS on a true-empty run (fixed in spec-108/ec1610b); the actually-scheduled EnrichRestaurants::enrichThrottled() (routes/console.php's 'restaurants:enrich --throttled' entry) already always returns SUCCESS regardless of quota_exhausted/cap-reached — the OTHER FAILURE returns in EnrichRestaurants.php (enrichSingleCity/enrichAllCities/enrichDiscoveredCities/handle's missing-city-arg check) are manual-CLI-only paths never wired into the scheduler, and are genuine operator-misuse errors (empty city arg, unresolvable city, no cuisines seeded) — leave them as FAILURE, do not touch. (3) quota:status scheduling — skip entirely, do not schedule it; uptime:canary (already every 15 min) already has an embedded SerpApi quota check via SerpApiCallLog with a consecutive-degraded webhook alert, which already covers the threshold-alarm need the spec wanted; adding a separate scheduled quota:status would be redundant. (4) ExternalApiCache::stats() overcounting — already resolved architecturally: stats()['serpapi_calls_last_30d'] is now explicitly documented (see its docblock) as a cache-inventory-only stat, NOT used for any quota decision — all real quota logic uses SerpApiCallLog::countLast30Days() instead (spec-106). Do not modify stats() or add a non-empty filter to it. (5) scheduler.log logrotate — already shipped in .github/workflows/deploy.yml (the 'Ensure Laravel scheduler cron + logrotate' step installs deploy/logrotate-ipop360 to /etc/logrotate.d/ipop360 on every deploy). Do the ONE remaining genuinely-unaddressed item only: (6) app/Console/Commands/UptimeCanary.php's checks are DB connectivity + upstream provider homepages (BizData/Overpass) + a social-scrape DB query + the SerpApi quota check — it NEVER exercises the app's own live search API, so a broken read path (e.g. a regression in RestaurantController, scopeNearby, or the scoring pipeline — exactly the kind of thing PR #161/spec-095 just touched) would pass the canary while genuinely down. Add a self-check: an internal Http::get(config('app.url').'/api/restaurants?cuisine=chinese&lat=30.6199783&lng=-88.1967496') call (mirror the exact query + min-result-count pattern already used in .github/workflows/deploy.yml's 'Verify deployment' step around line 431 — Mobile AL, a cache-warm known-good query) asserting HTTP 200 AND a non-empty 'data' array (use the same DEPLOY_VERIFY_MIN_RESULTS-style threshold or a simple non-empty check, your call — keep it simple). On failure (non-200, missing data key, or empty data array), set $status to at least 'degraded' (matching the existing pattern used for the other soft checks in this file — do not escalate to 'critical', which only the DB check currently uses and which fails the scheduled run's exit code) and populate $checks['self_api'] with a descriptive failure message; on success set $checks['self_api'] = 'ok' with the result count. Use a short HTTP timeout (5s like the other checks) and wrap in try/catch like the other checks so a canary-check failure never crashes the canary itself. Add a new test in tests/Feature/ (find the existing UptimeCanary test file first and follow its Http::fake() conventions) covering: self-check success sets checks.self_api=ok, self-check HTTP failure or empty-data sets status to degraded (not critical) and populates a failure message, and confirm the existing critical-only-on-DB-failure behavior is unchanged. All existing backend tests must stay green.

## State
Added self_api check to UptimeCanary (item 6, the last unaddressed spec-096 item): internal GET of app.url/api/restaurants (chinese/Mobile query, mirrors deploy.yml), asserts 200 + non-empty data; soft failure → degraded (never critical). 3 new Feature tests + updated test_command_reports_ok to fake the self-api call. Pint/phpstan/uptime tests green. Next: full `composer test` gate + re-read spec to confirm goal fully closed.

## Log
- [2026-09-01] Added self_api self-check to app/Console/Commands/UptimeCanary.php (spec-096 item 6). try/catch + 5s timeout, non-200/empty-data → status degraded (not critical) + $checks['self_api'] message; ok → 'ok (N results)'. New tests in UptimeCanaryCommandTest: ok-on-results, degraded-on-empty-data, degraded-on-HTTP-failure; existing healthy test now fakes the self-api call. Gotcha: the self-api GET hits the real app url, so every canary test must Http::fake that route or the first healthy test fails.

---

## Goal (merged from master — spec-095, already shipped)
Fix spec-095 (specs/095-read-path-db-indexes-and-queries.md), UPDATED SCOPE — verified against current code on 2026-09-01, one item from the original spec is already done and must NOT be touched: 'allowLiveSerpApiFetch' in app/Services/LiveSearchService.php now uses SerpApiCallLog::countLast30Days() (a single targeted query), not ExternalApiCache::stats() — that part of the spec is obsolete, skip it entirely, do not modify SerpApi quota/circuit-breaker logic at all. Do these 4 still-valid items only: (1) Add a standalone index on cuisines.slug via a new migration (currently only a composite unique(['category_id','slug']) in database/migrations/2026_06_06_171950_create_cuisines_table.php — every ?cuisine=slug lookup in RestaurantController full-scans). (2) Add indexes on external_api_cache.expires_at and external_api_cache.fetched_at via a new migration (database/migrations/2026_06_06_171950_create_external_api_cache_table.php has no index on either — speeds up apicache:gc, findByKey, and cache lookups). (3) app/Http/Controllers/RestaurantController.php's snapshotLiveResults() (~line 96) loops over up to 20 live-search results calling ExternalApiCache::storeByKey() per venue (each an updateOrCreate = SELECT+UPSERT round-trip) — wrap the loop in a single DB::transaction to cut it from N separate commits to 1 (keep the per-row logic as-is, including storeByKey's empty-data-shorter-TTL branch — this is a round-trip/commit-batching win, not a query-shape change). (4) app/Models/Restaurant.php's scopeNearby() (~line 267) does ->selectRaw('*, ... AS distance', ...) — pulling every column including heavy unused JSON (photos, score_breakdown) for every bbox candidate on every list/search request. Trim to an explicit column list instead of '*': it MUST keep every column PopularityScoreService's calculateBreakdownWithAggregatesFromEloquent and the card/list UI need — id, slug, name, address, phone, city, state, latitude, longitude, price_range, website_url, photo_url, features, cuisines relation keys, is_active, social_links_count, google_rating, google_review_count, has_award, website_clicks_count, pageviews_count, social_link_clicks_count, menu_click_count, directions_clicks_count, call_clicks_count — only drop genuinely heavy/unused-for-list columns like the photos gallery array and score_breakdown JSON (those are only needed on the single-restaurant show() page, which doesn't use scopeNearby). scopeNearby is called from app/Services/UnifiedSearchService.php:109 and app/Http/Controllers/RestaurantController.php:580 — both list/search contexts, never show(). This is the highest-risk item: verify via the existing ranking/scoring test suite that trimming introduces ZERO score changes (data_completeness signal must stay identical) before committing — if in doubt about whether a column is needed, keep it, do not guess. Add or extend tests: a migration/schema test asserting both new indexes exist, a test asserting snapshotLiveResults wraps in one transaction (mock/spy DB::transaction call count = 1, not N), and a test asserting scopeNearby's result set still contains every field the existing PopularityScoreServiceTest / ranking tests already assert on (no new nulls introduced for previously-populated fields). All existing backend tests must stay green — this is a zero-behavior-change performance pass, not a refactor of scoring logic.

## State
Item 4 done: scopeNearby() select trimmed from `*` to explicit column list (all scoring/card fields kept — description, place_types, popularity_score, rank_change, yelp_*, google_*, engagement counts, postal_code, source — only heavy JSON photos/score_breakdown/ai_metadata/opening_hours dropped). New RestaurantNearbyScopeTest (3 tests) locks keep-list non-null + heavy JSON absent + distance in km. Full suite 1114 pass, pint + phpstan clean. All 4 spec items complete; goal done pending loop's final `composer test` gate.

## Log
- [2026-09-01] Item 4: scopeNearby select trimmed to explicit list; RestaurantNearbyScopeTest (3 pass); full suite 1114 pass, pint + phpstan clean.
- [2026-09-01] Item 3: snapshotLiveResults loop in one DB::transaction + depth-tracking test; ControllerTest 44 pass.
- [2026-09-01] Item 1+2: migration for cuisines.slug + external_api_cache.expires_at/fetched_at indexes; ReadPathIndexesTest (3 pass); migrate:fresh clean.

---

## Goal (merged from master — spec-093, already shipped)
Fix spec-093 (specs/093-osm-cuisine-tag-cuisine-match.md): OverpassService::normalizeResults (app/Services/OverpassService.php ~lines 419-467) routes the OSM cuisine= tag only into the cuisines array and sets description => null, so LiveSearchService::stampCuisineMatchStrength (~830-848) and filterByCuisineRelevance (~882-954) — which only read name + place_types + description — never see it. Every Overpass row collapses to name-only cuisine matching: a venue OSM-tagged cuisine=thai;asian but named e.g. 'Siam Palace' gets cuisine_match=0.0 on a ?cuisine=thai search instead of reaching the 0.5 type+description tier, and can be wrongly dropped by filterByCuisineRelevance on a rival-cuisine search. Fix: in OverpassService::normalizeResults, also populate description (and/or place_types) from the OSM cuisine/amenity tags using the existing CuisineMatcher/CuisineScope (the single accessor for config/cuisine-keywords.php) — do not add a new denylist or duplicate matching logic. This must only ADD a positive stamp for on-cuisine rows; off-cuisine rows and previously-passing rows must be unchanged (recall-safe, no new drops). Gate stays behind the existing ranking.cuisine_match kill-switch (already default on). Zero SerpApi quota impact (Overpass path only). Add a new unit test (tests/Unit/LiveSearchScoringTest.php or a new OverpassServiceTest) with an OSM fixture cuisine=thai;asian on a non-Thai name, asserting cuisine_match >= 0.5 (was 0.0), plus a genuinely off-cuisine OSM venue asserting its stamp is unchanged. Confirm filterByCuisineRelevance keeps an on-cuisine-tagged OSM row it would previously have rival-dropped.

## State
GOAL COMPLETE. iterate-3 verified all spec-093 ACs: `OverpassService::normalizeResults` seeds description/place_types from OSM cuisine/amenity tags; stamp reaches 0.5 tier for tagged-but-vaguely-named rows, off-cuisine rows unchanged, relevance filter keeps the tagged row. 83 targeted tests green (OverpassServiceTest + LiveSearchScoringTest). Ready for branch hardening + merge.

## Log
- 2026-09-01: iter3 re-verified spec-093 end-to-end green (83 tests, 280 assertions); all ACs met.
- 2026-09-01: iter2 hardened spec-093 test (deterministic rival-drop + 0.0 ambiguous stamp, 5 assertions) + fixed phpstan error; repo phpstan 0 errors.
- 2026-09-01: OverpassService seeds description/place_types from OSM cuisine/amenity tags (spec-093). 83 targeted tests + full suite (1110 passed) + phpstan + pint green.
